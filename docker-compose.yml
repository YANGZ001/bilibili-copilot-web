services:
  bilibili-copilot-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bilibili-copilot-web
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.local
