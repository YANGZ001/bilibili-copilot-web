export interface PromptTemplate {
  id: string
  name: string
  description: string
  instruction: string
}

export const defaultTemplates: PromptTemplate[] = [
  {
    id: 'outline',
    name: '时间线大纲',
    description: '按时间线生成详细分层大纲。',
    instruction: `请根据下面的音视频字幕生成非常详细的 Markdown 总结大纲。

要求：
1. 用中文回答，内容必须具体、可复查，不能只写泛泛的标题。
2. 必须使用下面的结构：
   # [MM:SS] 大章节标题
   [<image>@MM:SS]
   ## [MM:SS] 小章节标题
   - 核心结论，观点、结论、结构，不要展开细节
   - 虽然不展开细节，但是提到过的点你都得提到，不要遗漏
   ### [MM:SS] 三级章节标题
   - 如果小章节内容较多，可以继续细分成三级标题，但请优先保证二级标题的清晰和完整。
3. 大纲必须有清晰层次，做到详细而不繁杂；明确而不冗余
4. 每个大章节和每个小章节标题都必须以时间戳开头，例如：# [02:15] 大章节标题、## [03:08] 小章节标题。
7. 大章节图片由 AI 决定：每个大章节标题下方必须输出 1 行图片标签，例如：[<image>@02:18]，用于表示该大章节的代表画面。
8. 小章节不要输出图片标签；小章节图片由用户之后手动插入。
9. 图片标签必须独占一行，只能放在大章节标题下方
10. 所有时间点必须使用 [MM:SS] 或 [HH:MM:SS] 格式；时间范围可使用 [MM:SS-MM:SS]，但标题开头优先使用单个起始时间点。
11. 不要输出 JSON，不要使用表格。`,
  },
  {
    id: 'summary',
    name: '总结音视频',
    description: '提炼核心结论、关键概念与可追问问题，侧重理解而非结构。',
    instruction: `请根据下面的音视频字幕生成以理解为导向的 Markdown 总结。

要求：
1. 用中文回答。
2. 必须使用下面的结构（禁止添加时间线大纲）：
   # 音视频总结
   ## 核心结论
   ## 关键概念
   ## 可追问问题
3. 核心结论：3-5 条最重要的观点或结论，直接可引用，不加时间戳。
4. 关键概念：解释音视频中出现的重要概念、术语或框架，每条聚焦一个概念。
5. 可追问问题：列出 3-5 个值得深入探讨的问题。
6. 不要输出时间线大纲——时间线由「总结大纲」负责。
7. 不要输出 JSON，不要使用表格。`,
  },
  {
    id: 'learn',
    name: '学以致用',
    description: '提炼要点、行动清单与记忆卡片，帮助主动学习。',
    instruction: `请根据下面的音视频字幕生成学习导向的 Markdown 笔记。

要求：
1. 用中文回答。
2. 始终输出核心要点；行动清单和记忆卡片仅在内容相关时输出，不相关则整个部分省略。
3. 必须按以下顺序和格式输出：
   ## 核心要点
   - 3-5 条简短的核心结论或知识点
   ## 行动清单
   - 每条以动词开头，描述一个具体可执行的步骤或建议
   ## 记忆卡片
   **Q:** 问题
   **A:** 答案
4. 时间戳可选：如某个要点或行动对应明确片段，可加 [MM:SS]，但不强制。
5. 不要输出 JSON，不要使用表格。`,
  },
]

export function findTemplate(id: string): PromptTemplate {
  return defaultTemplates.find((item) => item.id === id) || defaultTemplates[0]
}

export function buildChatSystemPrompt(subtitleText: string, videoTitle: string): string {
  return [
    `你是一个严谨的音视频字幕问答助手。正在就音视频《${videoTitle || '当前音视频'}》的内容提供答疑。`,
    '你的回答必须严格基于提供的字幕内容。如果用户问到的信息在字幕中没有提及，请明确告知用户。',
    '请使用 Markdown 格式回答，可以引用真实的时间戳来帮用户定位。',
    '如果需要，可以输出图片标签表示当时音视频画面，格式为独占一行的：[<image>@MM:SS]，例如 [<image>@03:45]。',
    '请务必保证图片标签和时间戳完全对应字幕中真实出现的时间点，绝对不要编造。',
    '\n--- 音视频字幕上下文开始 ---',
    subtitleText,
    '--- 音视频字幕上下文结束 ---',
  ].join('\n')
}
